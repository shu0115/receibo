
<br />
　ユーザ数：<%= @users.count %><br />
<br />
<% @users.each_with_index{ |user,index| %>
　<%= index %>：<%= user.login %>：<%= user.name %>：<%= user.created_at.strftime("%Y/%m/%d %H:%M:%S") %><br />
<% } %>
<br />
